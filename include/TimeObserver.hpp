#ifndef __TIMEOBSERVER_HPP__
#define __TIMEOBSERVER_HPP__

#include <string>

namespace TimeObserver
{
    class TimeObserver
    {
        public:
            TimeObserver();
            std::string now();
    };
}

#endif

